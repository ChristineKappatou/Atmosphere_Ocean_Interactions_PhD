
This folder contains the modules needed to run each one of the scripts.

There is a list in both scripts> nird_scripts and scripts> local_scripts that describes which modules are needed for each script.
